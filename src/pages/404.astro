---
---

<html lang="sv">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <meta name="generator" content={Astro.generator} />
    <title>404 - Kotlett Ipsum</title>
    <style>
        @import url('https://fonts.googleapis.com/css2?family=Bebas+Neue&family=Oswald:wght@400;600;700&display=swap');

        * {
            margin: 0;
            padding: 0;
            box-sizing: border-box;
        }

        body {
            font-family: 'Oswald', sans-serif;
            background: #2a1810;
            background-image:
                repeating-linear-gradient(90deg,
                    transparent,
                    transparent 2px,
                    rgba(0,0,0,0.1) 2px,
                    rgba(0,0,0,0.1) 4px),
                linear-gradient(180deg, #3d2517 0%, #2a1810 100%);
            min-height: 100vh;
            padding: 2rem;
            display: flex;
            justify-content: center;
            align-items: center;
        }

        .container {
            background: #f5f1e8;
            background-image:
                repeating-linear-gradient(0deg,
                    transparent,
                    transparent 40px,
                    rgba(139, 90, 43, 0.03) 40px,
                    rgba(139, 90, 43, 0.03) 80px);
            border: 8px solid #8b5a2b;
            box-shadow:
                0 0 0 4px #f5f1e8,
                0 0 0 8px #d4a574,
                0 20px 60px rgba(0, 0, 0, 0.5),
                inset 0 2px 4px rgba(139, 90, 43, 0.1);
            padding: 3rem;
            max-width: 800px;
            width: 100%;
            position: relative;
            text-align: center;
        }

        .container::before {
            content: '';
            position: absolute;
            top: 1rem;
            left: 1rem;
            right: 1rem;
            height: 3px;
            background: repeating-linear-gradient(90deg,
                #8b5a2b 0px,
                #8b5a2b 10px,
                transparent 10px,
                transparent 20px);
        }

        .error-code {
            font-size: 10rem;
            font-family: 'Bebas Neue', sans-serif;
            color: #8b2e0b;
            line-height: 1;
            margin-bottom: 1rem;
            text-shadow:
                3px 3px 0px rgba(139, 46, 11, 0.2),
                -1px -1px 0px rgba(255, 255, 255, 0.5);
            letter-spacing: 10px;
        }

        h1 {
            color: #8b2e0b;
            margin-bottom: 1.5rem;
            font-size: 2.5rem;
            font-family: 'Bebas Neue', sans-serif;
            letter-spacing: 4px;
            text-transform: uppercase;
            text-shadow:
                2px 2px 0px rgba(139, 46, 11, 0.2),
                -1px -1px 0px rgba(255, 255, 255, 0.5);
        }

        .message {
            color: #5d3a1a;
            font-size: 1.3rem;
            margin-bottom: 2rem;
            line-height: 1.8;
            font-weight: 400;
            padding: 0 2rem;
        }

        .funny-messages {
            background: rgba(139, 90, 43, 0.08);
            padding: 2rem;
            border: 3px dashed #8b5a2b;
            border-radius: 4px;
            margin-bottom: 2rem;
        }

        .funny-messages p {
            color: #5d3a1a;
            font-size: 1.1rem;
            margin-bottom: 1rem;
            font-style: italic;
            line-height: 1.6;
        }

        .funny-messages p:last-child {
            margin-bottom: 0;
        }

        .meat-icon {
            font-size: 5rem;
            margin-bottom: 1rem;
            opacity: 0.6;
            animation: float 3s ease-in-out infinite;
        }

        @keyframes float {
            0%, 100% {
                transform: translateY(0px);
            }
            50% {
                transform: translateY(-20px);
            }
        }

        .home-btn {
            display: inline-block;
            margin-top: 1.5rem;
            padding: 1.2rem 3rem;
            background: #8b2e0b;
            color: #f5f1e8;
            border: 4px solid #5d1f08;
            border-radius: 2px;
            font-size: 1.3rem;
            font-weight: 700;
            cursor: pointer;
            text-decoration: none;
            text-transform: uppercase;
            letter-spacing: 3px;
            font-family: 'Bebas Neue', sans-serif;
            box-shadow: 0 4px 0 #5d1f08,
                        0 6px 12px rgba(0, 0, 0, 0.3);
            transition: all 0.2s;
        }

        .home-btn:hover {
            background: #a83b14;
            transform: translateY(-2px);
            box-shadow: 0 6px 0 #5d1f08,
                        0 8px 16px rgba(0, 0, 0, 0.4);
        }

        .home-btn:active {
            transform: translateY(2px);
            box-shadow: 0 2px 0 #5d1f08,
                        0 4px 8px rgba(0, 0, 0, 0.3);
        }

        @media (max-width: 600px) {
            .container {
                padding: 2rem 1.5rem;
            }

            .error-code {
                font-size: 6rem;
                letter-spacing: 5px;
            }

            h1 {
                font-size: 2rem;
                letter-spacing: 2px;
            }

            .message {
                font-size: 1.1rem;
                padding: 0 1rem;
            }

            .funny-messages {
                padding: 1.5rem;
            }

            .funny-messages p {
                font-size: 1rem;
            }

            .meat-icon {
                font-size: 3.5rem;
            }

            .home-btn {
                font-size: 1.1rem;
                padding: 1rem 2rem;
                letter-spacing: 2px;
            }
        }
    </style>
</head>
<body>
    <div class="container">
        <div class="meat-icon">🥩</div>
        <div class="error-code">404</div>
        <h1>Sidan Hittades Inte</h1>
        <p class="message">
            Kotletten du söker finns i ditt hjärta (eller på grillen)
        </p>

        <div class="funny-messages">
            <p>🔪 Den här sidan har blivit nedskuren från menyn.</p>
            <p>🍖 Tyvärr är den här biten kött slut för dagen.</p>
            <p>🔥 Denna URL har grillats lite för länge och är nu helt svedd.</p>
            <p>🥓 Kanske har någon redan tagit sista kotletten?</p>
        </div>

        <a href="/" class="home-btn">Tillbaka till Slakteriet</a>
    </div>
</body>
</html>
